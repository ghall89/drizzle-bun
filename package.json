{
  "name": "drizzle-bun",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "read": "bun run index.ts",
    "generate": "bun run index.ts --create",
    "push": "bunx drizzle-kit push",
    "migrate": "bunx drizzle-kit generate && bunx drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.22.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@bugsnag/cuid": "^3.1.1",
    "@faker-js/faker": "^8.4.1",
    "better-sqlite3": "^11.0.0",
    "drizzle-orm": "^0.31.0"
  }
}
